version: '3.8'
services:
  web:
    build: .
    ports: ["5000:5000"]
    env_file: .env
    depends_on: [db]
  db:
    image: mysql:8.0
    env_file: .env
    volumes:
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - db_data:/var/lib/mysql
volumes:
  db_data: